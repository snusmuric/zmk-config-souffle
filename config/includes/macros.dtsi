/ {
    macros {
    // 	lock_sym_layer: lock_sym_layer {
    // 		label = "lock_sym_layer";
    // 		compatible = "zmk,behavior-macro";
    // 		#binding-cells = <0>;
    // 		bindings = <&tog SYM &kp K_CANCEL>;
    // 	};

     layer_switch_macro: layer_switch_macro {
         compatible = "zmk,behavior-macro-one-param";
         #binding-cells = <1>;
         bindings
             = <&to BASE>
             , <&macro_param_1to1>
             , <&tog LAYER>     // Then switch to the specified layer
             ;
     };
    };
};
